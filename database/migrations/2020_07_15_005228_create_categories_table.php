<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

class CreateCategoriesTable extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::create('categories', function (Blueprint $table) {
            $table->id()->comment('Identificador de la categoria en la tabla.');
            $table->string('name')->unique()->comment('Nombre de la categoria.');
            $table->timestamps();
            $table->softDeletes()->comment('Campo que indica la fecha en la que ha sido eliminada la categoria.');
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::dropIfExists('categories');
    }
}
